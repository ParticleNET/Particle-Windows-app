<UserControl
	x:Class="Particle.Common.Controls.LoginControl"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:Particle.Common.Controls"
	xmlns:converts="using:Particle.Common.Converters"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d"
	DataContext="{Binding LoginViewModel, Source={StaticResource Locator}}"
	d:DesignHeight="300"
	d:DesignWidth="400">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="MainDictionary.xaml" />
			</ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <StackPanel>
            <TextBlock Text="Partical.io account login" Style="{StaticResource SubheaderTextBlockStyle}" />
            <TextBlock Text="Email Address" />
            <TextBox Text="{Binding Username}" />
            <TextBlock Text="Password" />
            <TextBox Text="{Binding Password}" />
            <ToggleSwitch IsOn="{Binding RememberPassword}">
                <TextBlock Text="Remember Password" />
            </ToggleSwitch>
            <Button x:Name="LoginAction" Tapped="LoginAction_Tapped">Login</Button>
        </StackPanel>
</UserControl>
